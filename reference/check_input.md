# Check and Preprocess Input Data

This function checks whether input data is a data frame, matrix, vector,
or factor variable, and converts it to a numeric matrix.

## Usage

``` r
check_input(input)
```

## Arguments

- input:

  A data frame, matrix, vector, or factor variable to be converted to a
  numeric matrix.

## Value

A numeric matrix corresponding to the input data frame.
